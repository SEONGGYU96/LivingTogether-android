<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seoultech.livingtogether_android">
    <!-- For Bluetooth low energy usage -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- For using only BLE device -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.seoultech.livingtogether_android.debug.scan.ScanServiceTest"></activity>

        <service
            android:name=".service.ScanService"
            android:exported="false" /> <!-- 다른 앱에서 해당 서비스를 실행하지 못하도록 -->
        <activity android:name=".ui.profile.EditProfileActivity" />
        <activity android:name=".ui.nok.AddNOKActivity" />
        <activity android:name=".ui.nok.NOKListActivity" />
        <activity android:name=".ui.scan.ScanActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity
            android:name=".ui.SplashActivity"
            android:noHistory="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>